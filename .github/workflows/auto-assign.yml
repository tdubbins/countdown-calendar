name: Auto Assign Issues
on:
  issues:
    types: [opened]
jobs:
  auto-assign:
    runs-on: ubuntu-latest
    steps:
      - name: Assign issue to creator
        uses: actions/assign-issues@v1.2.1
        with:
          assignees: tdubbins
          token: ${{ secrets.GITHUB_TOKEN }}
