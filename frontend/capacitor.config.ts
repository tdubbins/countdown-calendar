import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'advent-calendar-frontend',
  webDir: 'dist'
};

export default config;
